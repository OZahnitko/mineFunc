name: Faker
on: push
jobs:
  test:
    name: Test the application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Run test
        run: npm run test

      - name: Show the dir
        run: ls -la
